/*#include <iostream>
#include <deque>
#include <string>
using namespace std;

int main()
{
    deque <int> as;
    string s;
    getline(cin, s);
    as.push_back(s[3]-'0');
    cout << as.back() << endl;
    as.push_back(2);
    as.pop_back();
    cout << as.front() << " " << as.back();
}
*/
